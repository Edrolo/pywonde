"""
Wonde API

API Docs

The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""


from __future__ import annotations

import json
import pprint
import re  # noqa: F401
from typing import Any, ClassVar, Dict, List, Optional, Set

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from pydantic_core import to_jsonable_python
from typing_extensions import Self

from wonde.models.school_region_identifiers import SchoolRegionIdentifiers


class SchoolRegion(BaseModel):
    """
    SchoolRegion
    """

    code: Optional[StrictStr] = Field(default=None, json_schema_extra={'examples': ['GBR']})
    domain: Optional[StrictStr] = Field(
        default=None, json_schema_extra={'examples': ['api.wonde.com']}
    )
    school_url: Optional[StrictStr] = Field(
        default=None,
        json_schema_extra={'examples': ['https://api.wonde.com/v1.0/schools/A1300691890']},
    )
    identifiers: Optional[SchoolRegionIdentifiers] = None
    __properties: ClassVar[List[str]] = ['code', 'domain', 'school_url', 'identifiers']

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of SchoolRegion from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set()

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of identifiers
        if self.identifiers:
            _dict['identifiers'] = self.identifiers.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of SchoolRegion from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                'code': obj.get('code'),
                'domain': obj.get('domain'),
                'school_url': obj.get('school_url'),
                'identifiers': SchoolRegionIdentifiers.from_dict(obj['identifiers'])
                if obj.get('identifiers') is not None
                else None,
            }
        )
        return _obj
